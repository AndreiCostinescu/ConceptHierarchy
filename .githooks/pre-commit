#!/usr/bin/env sh
# .githooks/pre-commit
#
# Delegates all pre-commit checks to the pre-commit framework, which is the
# single source of truth (ruff formatting, linting, and custom hooks).
#
# One-time setup:
#   make setup          (installs dev deps and registers git hooks)

exec pre-commit run --hook-stage pre-commit
